version: '3.8'
services:
  tpv-backend:
    build: .
    container_name: tpv
    ports: ["5000:5000"]
    environment:
      - TPV_SECRET_KEY=${TPV_SECRET_KEY:-dev}
      - TPV_DEMO_PASSWORD=${TPV_DEMO_PASSWORD:-demo}
      - TPV_PORT=5000
    restart: unless-stopped
